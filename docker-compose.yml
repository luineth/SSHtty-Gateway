services:
  sshtty-gateway:
    image: ghcr.io/luineth/sshtty-gateway:main
    container_name: sshtty-gateway
    #environment:
    #  -DEFAULT_DEST=user@example.com # Optional speed dial host
    ports:
      - "23:2323"
    #Optionally, mount an .ssh folder to maintain known hosts and an ssh key.
    #Note the key must be named `id_dropbear`. Generate with
    #`dropbearkey -t ed25519 -f path/to/.ssh/id_dropbear` then copy to
    #remote authorized_keys file.
    #volumes:
    #  - path/to/.ssh:/home/gateway/.ssh
    restart: unless-stopped
